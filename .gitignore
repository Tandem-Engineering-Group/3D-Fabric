# Large / third-party / local-only — never commit (public repo, 50MB hard cap)
weights/
vendor/
logs/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
.venv-ai/
venv/

# Blender
*.blend1
*.blend2

# Archives & model weights (belt-and-suspenders)
*.zip
*.7z
*.safetensors
*.ckpt
*.pth
*.bin

# Local-only business documents — public repo carries no personal data
*.pdf

# render intermediates (mp4s ship, raw frames don't — hundreds of MB)
designs/renders/turntable_frames/
designs/renders/reel_frames/
designs/renders/orbit_frames/

# node (deck tooling only)
node_modules/
package.json
package-lock.json

# OS noise
Thumbs.db
desktop.ini
